// ini childern component
import React from 'react';

// props disini adalah variabel bebas
const Testing = (props) => {

    return (
        <React.Fragment>
            <h1>{props.title}</h1>
            <p>{props.description}</p>
        </React.Fragment>
    )
}

export default Testing;

// =-= documentation deacourse 14 Agustus 2022 -=- //